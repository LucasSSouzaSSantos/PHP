<?php

/* 
 * Operadores Relacionais
 * 
 * Maior                    $a > $b
 * Menor                    $a < $b
 * Maior ou Igual           $a >= $b
 * Menor ou Igual           $a <= $b
 * Diferente                $a <> $b ou $a != $b
 * Igual                    $a == $b
 * idêntico                 $a ==== $b
 * 
 * Operador Unário
 * expressão ? verdadeiro: falso
 * $maior = $a > $b? $a : $b
 * 
 * Operador Lógico
 * Operador E     and ou &&
 * Operador OU    or ou ||
 * Operador XOU   xor     
 * Operador Não   not ou !
 * 
 * 
 */

